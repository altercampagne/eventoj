<div class="btn btn-sm copy-btn p-0 text-muted" data-clipboard-text="{{ value }}" data-bs-toggle="tooltip" title="{{ tooltip|default('Copier la valeur') }}">
    {% block content %}
        <i class="fas fa-clipboard fa-fw"></i>
    {% endblock %}
</div>
