<div class="btn btn-sm copy-btn p-0 text-muted" data-clipboard-text="{{ value }}" data-toglle="tooltip" title="{{ tooltip|default('Copier la valeur') }}">
    <i class="fas fa-clipboard fa-fw"></i>
</div>
