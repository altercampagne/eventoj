<twig:ButtonPostWithConfirmation
    url="{{ url }}"
    disabled="{{ disabled|default(false) }}"
    buttonLabel="{{ buttonLabel|default('Supprimer') }}"
    btnClass="{{ btnClass|default(null) }}"
    modalTitle="{{ modalTitle|default('Confirmation de la suppression') }}"
    modalButtonClass="{{ modalButtonClass|default('btn btn-danger') }}"
    modalButtonLabel="{{ modalButtonLabel|default('J\'ai bien compris et je confirme la suppression définitive')|raw }}"
/>
