<div class="d-flex justify-content-center align-items-center">
    <img{{ attributes.without('width', 'height', 'src') }}
        loading="lazy"
        width="100%"
        src="{{ this.url }}"
    />
</div>
